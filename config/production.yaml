pipeline:
  tone: casual
  noise_level: 0.20
  seed: null

noise:
  typo_weight: 0.5
  filler_weight: 0.3
  drop_weight: 0.2
  contextual_noise: false
  adaptive_noise: false

style:
  persona_adaptation: false
  memory_based_variation: false

output:
  format: json
  path: output/synthetic_dataset.json
